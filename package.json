{
  "name": "nextjs-monorepo-example",
  "license": "MIT",
  "private": true,
  "homepage": "https://github.com/belgattitude/nextjs-monorepo-example",
  "repository": {
    "type": "git",
    "url": "https://github.com/belgattitude/nextjs-monorepo-example"
  },
  "author": {
    "name": "Vanvelthem Sébastien",
    "url": "https://github.com/belgattitude"
  },
  "keywords": [
    "example",
    "workspace",
    "nextjs",
    "next-transpile-modules"
  ],
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "yarn workspaces foreach -pt run clean",
    "typecheck": "yarn workspaces foreach -pt run typecheck",
    "test": "run-s test:*",
    "test:unit": "yarn workspaces foreach -pt run test:unit",
    "changeset": "changeset",
    "release": "yarn build && changeset publish",
    "deps:check": "npm-check-updates && yarn workspaces foreach run deps:check",
    "deps:update": "npm-check-updates -u && yarn workspaces foreach run deps:update",
    "prettier:all": "prettier './**/src/**/*.(ts|tsx|css|scss)' --write",
    "check:monorepo": "yarn dlx @yarnpkg/doctor ."
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.3.0",
    "@changesets/cli": "^2.14.1",
    "npm-check-updates": "^11.3.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  },
  "engines": {
    "node": ">=14",
    "yarn": ">=1.22.0",
    "npm": "please-use-yarn"
  }
}
