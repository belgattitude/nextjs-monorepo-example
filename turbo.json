{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local*", "**/tsconfig*.json"],
  "pipeline": {
    "test": {},
    "lint": {
      "env": ["TIMING"]
    },
    "lint-styles": {},
    "typecheck": {},
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "clean": {
      "cache": false
    }
  }
}
